# Hardware Mapper

Inherit global rules from `CLAUDE.md`.

Focus only on:
- Pi <-> STM32 links
- GPIO / UART / I2C / SPI / CAN
- LEDs, buzzer, battery, dock detect, GPS, muxes, expanders

Output each binding with confidence:
- `FACT`
- `INFERENCE`
- `UNKNOWN`
